# ネコーダー

ねこボイスシンセ、ネコーダー。  
チップチューン寄りの簡易シンセで、ねこっぽい鳴き声を作って遊べる Web アプリです。

## Features

- 音色: 白猫、茶トラ、キジトラ、黒猫、サバトラ
- 鳴き方: にゃーん、にゃっ、みゃう、なーお、みゃ
- モフ度: ピコ猫から超にゃーんまで連続調整
- ピッチ、長さ、ビブラート、音量、フラット度の調整
- パラメータのランダム化とリセット
- URL hash による設定共有
- Web MIDI キーボード入力
- WebMidiLink Level 1 受信
- ビルド後の `dist/index.html` はローカルで開くだけでも動作

## Usage

画面上のボタンで音色と鳴き方を選び、モフ度や各種パラメータを調整して `にゃーん！` ボタンを押します。

PC キーボードの `A S D F G H J K` でも演奏できます。  
Web MIDI 対応ブラウザでは、`MIDI OFF` ボタンから MIDI キーボード入力を有効化できます。

WebMidiLink 対応ホストから iframe や popup のシンセとして読み込むと、`midi,90,3c,64`
のような WebMidiLink メッセージで演奏できます。起動時の `link,ready`、
`link,reqpatch` / `link,setpatch,<query>` による現在設定の送受信にも対応しています。

### フラット度でメロディを弾く

`FLAT / フラット度` を上げると、鳴き声特有のしゃくり上げ・うねり・自動ビブラートが
その割合だけ抑えられ、100% でピッチが完全に平坦になります。同時に音程が平均律へ
スナップするので、どの音色でもそのままメロディが弾けます。

| 音色 | フラット度 100% のときの基準音 (ド鍵) |
| ---- | ------------------------------------- |
| 白猫 | B5 |
| 茶トラ | F#5 |
| キジトラ | A5 |
| 黒猫 | C#5 |
| サバトラ | C4 |

基準音は音色ごとの鳴き声の高さをそのまま使うため、ド鍵が C になるのはサバトラだけです。
鳴き方を変えると基準音も変わります (`pkMul` の分だけ移調します)。

### サバトラでメロディを弾く

音色「サバトラ」は固定フォルマントで子音ノイズの無い、ローファイ寄りの音色です。
しゃくり上げが無い代わりに出だしですくうように動くので、既定でもにゃん感が出ますが、
`FLAT / フラット度` を 100% にすると完全に平坦な音階楽器になります。

おすすめ設定は 鳴き方「みゃ」、モフ度 65%、抑揚「フラット」、フラット度 100%。
長い音を出したいときは鳴き方を「にゃーん」にしてデュレーションを上げます。

この音色はド = C4 なので、半音指定がそのまま音名になります。

| 半音 | 音名 | 画面鍵盤 / PC キー | MIDI note |
| ---- | ---- | ------------------ | --------- |
| +4   | E4   | ミ / `d`           | 64        |
| +5   | F4   | ファ / `f`         | 65        |
| +7   | G4   | ソ / `g`           | 67        |
| +9   | A4   | ラ / `h`           | 69        |
| +10  | B♭4  | (無し)             | 70        |

画面の 8 鍵盤は C メジャーのダイアトニックなので、B♭ のような黒鍵は MIDI キーボードか
WebMidiLink ホストから鳴らしてください。

## Development

```sh
npm ci
npm run dev
```

型チェック:

```sh
npm run typecheck
```

ビルド:

```sh
npm run build
```

`npm run build` は TypeScript の型チェック、Vite build、`dist/index.html` への JS/CSS インライン化を行います。

## Generated Audio

ネコーダーで合成した音声は自由に利用できます。  
商用・非商用、加工、再配布、作品への組み込みなど、用途を問わず利用できます。

## License

CC0 1.0 Universal.  
See [LICENSE](LICENSE).
